type: InlineMultiQuestion
id: test_6_1
access_rules:
    add_permissions:
        - see_answer_after_submission
prompt: |

    # 演習問題 6_1
    空欄をうめてください。

question: |

    　# 文字列を逆にして連結する関数を作成

    [[blank0]] :

    $~~~~$return str1[::-1] + str2[::-1]

    

    print(revcat('abc', '123'))


answers:

    blank0:
        type: ShortAnswer
        width: 25em
        required: True
        correct_answer:
        - <plain> def revcat(str1, str2)
        - <regex>\s*def\s*revcat\(str1,\s*str2\)\s*

